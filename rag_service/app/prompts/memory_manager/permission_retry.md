Your previous answer redundantly asked the user for permission. The application already provides one final Confirm action for a concrete proposal.

Return the concrete proposal now, or return `no_changes` when the requested state is already stored. Do not ask another question and do not return approval choices.
