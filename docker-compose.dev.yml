services:
  frontend:
    build:
      target: dev
    command: >
      sh -lc "[ -x node_modules/.bin/next ] || npm ci --no-audit --no-fund; npm run dev"
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - frontend_next:/app/.next

volumes:
  frontend_next:
